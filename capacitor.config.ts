import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'farmers-app-project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
